// Deprecated: the settings UI moved to a React webview shared by the sidebar
// view and the editor panel (see webview.ts + src/ui/). This file is kept only
// so older imports don't break; nothing references it anymore.

export {};
