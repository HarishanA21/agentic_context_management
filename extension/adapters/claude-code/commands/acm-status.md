---
description: Show the active ACM context-management profile and recent edits
---

Report the current ACM context-management state. Call the `acm` MCP server's
`status` tool, then briefly summarise:

1. Which preset / profile is active.
2. Which techniques are enabled (trimming, summarisation, image-recall, memory).
3. If the user asks, switch presets with the `set_profile` tool.

Keep it to a few lines.
