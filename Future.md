
MCPs and COnnectors

Multiple LLM providers (AWS bedrock, Openrouter, Azure, OpenAI, Google Studio..) -> Need to create a settings page and add configuration window to configure different providers
Like Seelct the available provide, enter the necessary credentials, enter the model name -> that model needs to be available and set in the chat

Create API Keys - Wrap the openrouter models with the same names, but our platfrom api keys exposing those models
Need to implement compact, summarization and my research emthods, so user can chnage it according to their needs

Demo UI
Raw text method + IMage for tools + Image for entire window

Cloudflare Typescript API Method




Modify the UI, enable light ad dark mode options
Clearly show difference between projects and chats, show porject contents propelry
In the main ui, context window circle button, show the percentage of token includen in that button, if the 50% context window is reached, show half orange borader of the cirecle in orange, update the circle outline according to the percentage, so users can understand the token without clicking and going inside
