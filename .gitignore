# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Env files
.env
ui/.env.local

# Next.js / UI
ui/node_modules/
ui/.next/
ui/out/
ui/next-env.d.ts

uv.lock


# Uploaded user files (per-session storage)
uploads/
.claude


current.md
note.txt
hello.txt

# macOS junk
.DS_Store

# Stray binaries / scratch artifacts
*.pdf

# Generated visual-method rasterizer output (debug dumps)
backend/visual_images/
*__read_project_file.png

# TS incremental build cache
ui/tsconfig.tsbuildinfo

# Planning docs — kept locally, not pushed
documents/
Future.md
PROJECT.md

# Generated extension docs/plans — kept locally, not pushed
# (extension/README.md is committed — pyproject.toml's readme field needs it
# in the checkout for the PyPI sdist/wheel build)
# (the vscode README.md and CHANGELOG.md are committed too — CI builds and
# publishes the .vsix from a fresh checkout, so they must be tracked to show
# on the Marketplace and Open VSX pages)
/EXTENSION_PLAN.md
/extension/PACKAGING.md
/extension/PRIVATE_DEPLOY.md
/extension/TESTING.md
/extension/adapters/claude-code/README.md
/extension/adapters/cursor/README.md
/extension/adapters/vscode/UI_PLAN.md