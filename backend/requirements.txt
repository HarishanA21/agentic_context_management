markdown
openai
python-dotenv
langchain
langchain-openai
langchain-anthropic
langgraph-checkpoint-sqlite
langgraph-checkpoint-postgres
psycopg[binary]
psycopg-pool
pyjwt[crypto]
fastapi
uvicorn[standard]
python-multipart

# File parsing
pypdf
python-docx
openpyxl

# Storage (S3-compatible: MinIO locally, AWS S3 / R2 in prod)
boto3

# Sandboxed workspaces — one backend used at a time via SANDBOX_BACKEND env var
docker          # backend=docker: talks to local Docker socket
e2b             # backend=e2b: managed Firecracker microVMs

# GitHub integration
PyGithub

# Multi-provider LLM adapters (Bedrock + Google; OpenAI/Anthropic/Azure
# already covered by langchain-openai and langchain-anthropic above)
langchain-aws
langchain-google-genai

# Tests
pytest

# MCP inventory — adapter for stdio + SSE servers, plus the raw Python
# SDK for streamable_http connections.
langchain-mcp-adapters>=0.1
mcp>=1.0

# Secret-blob encryption for stored MCP API tokens / env-var values.
cryptography

